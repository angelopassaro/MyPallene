global
    a : int = 0;
    b : int = 0;
    result: int
end

function main( ): nil
    "Inserisci due numeri:" ==>;
    a, b <== ;
    result = add( a, b );
    "Il risultato è: ", result ==>
end

function add( x : int , y : int ): int
  return x + y
end