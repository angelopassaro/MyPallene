global
    a : int = 0;
    result: int
end

function main( ): nil
    local
        x:int = 10;
        y:int = 5;

        result = execute(inc,x,x+y*2,inc(4));
        "result = ", result ==>
    end
end

//no type
function inc( x : float): int
  return x + 1
end