global
    a : int = 0;
    b : int = 0;
    result: int
end

function main( ): nil
    "Inserisci due numeri" ==>;
    a, b <== ;
    result = division( a, b );
    "Il risultato è: ", result ==>
end

function division( x : int , y : int ): int
  return x / y
end